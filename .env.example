# Domaine Traefik
SUBDOMAIN=earfood
DOMAIN_NAME=example.com

# Profil Docker Compose : "dev" ou "prod"
# - dev  : Vite dev server + HMR (git pull = changements instantanés, pas de rebuild)
# - prod : Build Vite complet + Nginx (optimisé pour la production)
COMPOSE_PROFILES=prod
